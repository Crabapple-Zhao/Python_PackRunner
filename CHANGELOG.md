# 更新日志（Changelog）

本文件记录本项目各版本的重要变化。

## [V1.2] - 2026-09-23

### 新增

- 打包 EXE 时自动注入 PyInstaller 运行时钩子（`--runtime-hook`），将 `--icon` 嵌入的 ICO 资源同步到 Tkinter `Tk` 窗口，无需修改目标脚本即可让打包后的 EXE 窗口显示自定义图标；
- 主窗口图标实时预览：选择或输入 ICO 路径后延迟 250ms 自动加载到当前窗口，清空后恢复 EXE 默认图标；
- `tempfile` 引入，运行时钩子以临时目录托管，打包进程结束后自动清理。

### 优化

- 将日志中"执行命令"的打印位置后移至运行时钩子注入之后，避免日志与实际命令不一致；
- 钩子目录在 `finally` 中清理，清理失败仅记录警告，不影响主流程。

## [V1.1] - 2026-09-21

### 新增

- 打包 EXE 支持自定义 `.ico` 图标，新增“图标文件”输入框、“选择图标”和“清除图标”按钮；
- 打包前检查图标后缀及文件是否存在，错误时提示并阻止打包；取消选择保持原设置，清除后恢复默认图标；
- 打包日志显示所选图标完整路径或“默认”。

### 修复

- 自动清理时保护本次使用的 ICO 源文件；若图标位于待清理构建目录内，则保留该目录并记录提示；
- 启动日志和依赖说明改为引用 `APP_VERSION`，避免界面版本号与程序版本不一致。

### 优化

- 使用原始图标绝对路径，通过 subprocess 列表参数传递 `--icon`，兼容中文、空格及括号，不复制 ICO 文件；
- 保留原有 uv 调用结构、Python 版本选择、依赖识别、单文件打包、隐藏控制台及成功后的清理逻辑；未选择图标时保持原打包参数；
- 源码入口由 `Python_Tool_V1.0.py` 调整为 `Python_Tool_main.py`，同步 README 中的版本、运行方式及图标使用说明。

## [V1.0] - 2026-09-18

首个版本。

### 新增

- 基于 tkinter + uv 的图形界面工具：选择脚本、自动识别依赖、运行脚本、一键打包 EXE；
- 基于 `ast` 解析 import 语句，自动提取第三方依赖；过滤标准库（兼容 Python 3.8+）以及脚本同目录下的本地模块/包；
- import 名 → 安装包名映射（`serial → pyserial`、`cv2 → opencv-python` 等 11 组常见映射）；
- Python 版本可选（3.8.20 ~ 3.13），默认保持 3.8.20；
- "停止任务"按钮，可中途终止正在运行/打包的任务；
- "重新识别"按钮，可随时重新分析脚本依赖；
- "清空日志"按钮；
- "打包 EXE 时隐藏控制台（--noconsole）"选项，默认开启；
- 执行前自动检查 uv 是否可用，并改善命令显示与异常提示；
- EXE 打包成功后自动清理对应的 build 中间目录和 `.spec` 文件，保留 `dist` 与最终 EXE。

### 修复

- 修复 Python 3.8 下 `queue` 等标准库被误判为第三方库的问题；
- 修复 Windows 下 subprocess 默认按 GBK 解码 uv 的 UTF-8 输出导致的崩溃（`'gbk' codec can't decode byte ...`）。

### 优化

- 后台线程不再直接操作 Tk 控件，提升 UI 稳定性；
- 运行/打包时将工作目录切换到目标脚本所在目录，兼容脚本内的相对路径资源；
- 子进程输出改为 UTF-8 优先、GB18030 兜底的容错解码。
